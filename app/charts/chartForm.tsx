'use client';

export default function ChartForm() {
  return (
    <div>Chart</div>

    //          -> Input tags receive variable values from database
    //           -> Input tags are stored in 'tag-area'
    //           -> Drag & Drop (browser API) of variables from 'tag-area' to x, y input fields of chart
  );
}
